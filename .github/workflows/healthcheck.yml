name: Healthcheck

on: 
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

  workflow_dispatch:

jobs:
  run_tests:
    uses: jhonata-santos/ruby-automation-api-template/.github/workflows/api/ruby-api-tests.yml@master
  